# 第八天作业
# 主题：用户管理持久化 API

# 作业要求：
# 1. 建立 users 表（id, name, role, active）。
# 2. 完成 GET /users。
# 3. 完成 POST /users。
# 4. 完成 PATCH /users/{id}/role。
# 5. 完成 GET /report。
# 6. 所有找不到资源的场景返回 404。
# 7. 代码可在 /docs 完整联调。

# 建议直接基于 day08/main.py 复制一份独立实现。
