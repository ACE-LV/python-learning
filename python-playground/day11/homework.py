# 第十一天作业
# 主题：把 day10 的单文件 API 拆成分层结构

# 作业要求：
# 1. schemas.py：只放 Pydantic 请求/响应模型。
# 2. models.py：只放内部数据对象。
# 3. services.py：只放增删改查和报表逻辑。
# 4. main.py：只放 FastAPI 路由。
# 5. 完成 list/get/create/update/delete/report。
# 6. 在 summary.md 写清楚每一层负责什么。
