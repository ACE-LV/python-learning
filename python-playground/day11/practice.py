# 第十一天练习题
# 主题：API 分层

# 练习 1：在 schemas.py 新增 UserUpdateName。

# 练习 2：在 services.py 新增 update_user_name(user_id, name)。

# 练习 3：在 main.py 新增 PATCH /users/{user_id}/name。

# 练习 4：在 services.py 新增 list_active_users()。

# 练习 5：在 main.py 新增 GET /active-users。

# 检查点：main.py 里不要直接修改 USERS，业务逻辑放 services.py。
